# upstream fastcgi_backend {
#    server   unix:/var/run/php-fpm/php-fpm.sock;
# }

server {
   listen 80;
   server_name localhost.local;
   
   # if needed as SESSION variable
   set $ROOT /usr/share/nginx/localhost;
}